version: '3.8'
services:
  
  redis:
    image: redis:5.0
    expose:
      - 6379
  django_app:
    build:
      context: .
      dockerfile: Dockerfile.app
    env_file:
      - .dev.env
    volumes:
      - .:/app
    ports:
      - "8000:8000"