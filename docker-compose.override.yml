services:
  django_app:
    env_file: .env
    environment:
      - ENV_MODE=development
    develop:
      watch:
        - action: sync
          path: .
          target: /app
          initial_sync: true